/*
  Kata-name => String repeat

Description => Write a function called repeatStr which repeats the given string string exactly n times

*/ //=========================================================

function repeatStr(n, s) {
  return s.repeat(n);
}

repeatStrSecond = (n, s) => s.repeat(n);
